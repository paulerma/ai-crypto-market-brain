name: Trend transition detector

on:
  push:
    paths:
      - '.github/workflows/trend-transition.yml'
      - '.github/scripts/show_both_direction_windows.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Show both direction windows
        run: python .github/scripts/show_both_direction_windows.py

      - name: Syntax check
        run: python -m py_compile app.py

      - name: Commit app changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py
          if git diff --cached --quiet; then
            echo "No app changes needed"
          else
            git commit -m "Show probable up and down timing windows"
            git push
          fi
