name: Simple all-timeframes market view

on:
  push:
    paths:
      - '.github/workflows/simple-all-timeframes.yml'
      - '.github/scripts/apply_simple_market_view.py'
      - '.github/scripts/apply_signal_dot.py'
      - '.github/scripts/apply_ultra_fast_simple.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply simple market view
        run: python .github/scripts/apply_simple_market_view.py

      - name: Apply main signal dot
        run: python .github/scripts/apply_signal_dot.py

      - name: Apply ultra-fast simple mode
        run: python .github/scripts/apply_ultra_fast_simple.py

      - name: Syntax check
        run: python -m py_compile app.py

      - name: Commit app changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py
          if git diff --cached --quiet; then
            echo "No app changes needed"
          else
            git commit -m "Speed up timeframe switching and add discreet stop"
            git push
          fi
