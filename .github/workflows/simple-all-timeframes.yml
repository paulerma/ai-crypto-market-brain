name: Simple all-timeframes market view

on:
  push:
    paths:
      - '.github/workflows/simple-all-timeframes.yml'
      - '.github/scripts/separate_signal_and_time_axis.py'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Separate signal and add TradingView-like time axis
        run: python .github/scripts/separate_signal_and_time_axis.py

      - name: Syntax check
        run: python -m py_compile app.py

      - name: Commit app changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py
          if git diff --cached --quiet; then
            echo "No app changes needed"
          else
            git commit -m "Separate signal from candles and show chart time"
            git push
          fi
